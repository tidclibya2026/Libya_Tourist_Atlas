import fs from 'node:fs';
import path from 'node:path';
const root=process.cwd(), file=path.join(root,'data/layers/tourist-villages-resorts.geojson'), app=fs.readFileSync(path.join(root,'assets/app.js'),'utf8'), failures=[]; let data;
try { data=JSON.parse(fs.readFileSync(file,'utf8')); } catch(e) { failures.push(`invalid-json:${e.message}`); data={features:[]}; }
const features=Array.isArray(data.features)?data.features:[]; if(data.type!=='FeatureCollection'||!features.length) failures.push('invalid-feature-collection'); const ids=new Set();
for(const feature of features){const p=feature.properties||{},c=feature.geometry?.coordinates;if(!String(p.name_ar||'').trim())failures.push(`${p.id}:empty-name`);if(!p.id||ids.has(p.id))failures.push(`${p.id}:duplicate-id`);else ids.add(p.id);if(feature.geometry?.type!=='Point'||!Array.isArray(c)||c.length<2||!c.every(Number.isFinite))failures.push(`${p.id}:invalid-coordinate`);else if(c[0]<9||c[0]>26||c[1]<19||c[1]>34)failures.push(`${p.id}:outside-libya`);if(/<(?:img|br|div|script|style)\b|<!\[CDATA\[/i.test(String(p.description_ar||'')))failures.push(`${p.id}:raw-html`);if(/^(?:[A-Za-z]:|file:)|\\/.test(String(p.source_kml||'')))failures.push(`${p.id}:private-path`);const images=Array.isArray(p.local_images)?p.local_images:[];if(p.image_count!==images.length)failures.push(`${p.id}:image-count`);for(const image of images)if(/^(?:[A-Za-z]:|file:|https?:)|\\/.test(image)||!fs.existsSync(path.join(root,image.replace(/^\//,''))))failures.push(`${p.id}:broken-local-image`)}
if(!/id:\s*'resorts'[\s\S]*?type:\s*'geojson'[\s\S]*?url:\s*'data\/layers\/tourist-villages-resorts\.geojson'/.test(app))failures.push('layer-not-registered');if(!/cluster:\s*true/.test(app))failures.push('cluster-not-configured');for(const field of ['properties.units','properties.capacity','properties.operational_status','properties.license_status'])if(!app.includes(field))failures.push(`popup-field:${field}`);if(/source_image_url[\s\S]{0,120}(?:directMedia|<img)/.test(app))failures.push('eager-remote-image');
const status=r=>failures.some(f=>r.test(f))?'FAIL':'PASS';
console.log(`RESORTS_GEOJSON_VALID = ${status(/invalid-json|invalid-feature-collection/)}`);console.log(`NO_EMPTY_RESORT_NAMES = ${status(/empty-name/)}`);console.log(`NO_DUPLICATE_RESORT_IDS = ${status(/duplicate-id/)}`);console.log(`ALL_RESORT_COORDINATES_VALID = ${status(/invalid-coordinate/)}`);console.log(`ALL_RESORTS_INSIDE_LIBYA_BOUNDS = ${status(/outside-libya/)}`);console.log(`NO_RAW_KML_HTML = ${status(/raw-html/)}`);console.log(`NO_PRIVATE_PATHS = ${status(/private-path/)}`);console.log(`NO_BROKEN_LOCAL_IMAGES = ${status(/broken-local-image/)}`);console.log(`IMAGE_COUNT_VALID = ${status(/image-count/)}`);console.log(`RESORTS_LAYER_REGISTERED = ${status(/layer-not-registered/)}`);console.log(`RESORTS_CLUSTERING_CONFIGURED = ${status(/cluster-not-configured/)}`);console.log(`RESORTS_POPUP_FIELDS_CLEAN = ${status(/popup-field/)}`);console.log(`NO_EAGER_REMOTE_IMAGE_LOADING = ${status(/eager-remote-image/)}`);console.log(`FAILED = ${failures.length}`);if(failures.length){console.error(failures.slice(0,100).join('\n'));process.exitCode=1;}
