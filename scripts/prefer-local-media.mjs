import fs from 'node:fs';const file='assets/media-utils.js';let s=fs.readFileSync(file,'utf8');const old=`return [...new Set(found.map(v=>normalizeMediaUrl(v,kmlFileUrl,baseUri)).filter(v=>v&&(IMAGE_EXT.test(v)||/^data:image\\//i.test(v)||/googleusercontent|mymaps/i.test(v))))];`;const next=`const unique=[...new Set(found.map(v=>normalizeMediaUrl(v,kmlFileUrl,baseUri)).filter(v=>v&&(IMAGE_EXT.test(v)||/^data:image\\//i.test(v)||/googleusercontent|mymaps/i.test(v))))],local=unique.filter(v=>/\\/assets\\/images\\//i.test(v));return local.length?local:unique;`;if(!s.includes(old))throw new Error('extract return not found');fs.writeFileSync(file,s.replace(old,next));console.log('Local media now takes precedence.');
